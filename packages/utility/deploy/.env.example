# SiteFast Configuration

# Server Ports
PORT=3000           # Proxy server port (serves deployed sites)
API_PORT=3001       # API server port
HOST=localhost

# Domain Configuration
BASE_DOMAIN=localhost  # Base domain for deployments (e.g., myapp.localhost)

# Data Directories
DATA_DIR=./data    # Where to store all data
